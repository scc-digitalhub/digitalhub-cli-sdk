// Add basic authentication to the API

// user:password in base64 da passare all'eader in httpcore.go dopo questo

if tok := httpCore.coreConfig.AccessToken; tok != "" {
	req.Header.Set("Authorization", "Bearer "+tok)
}


usare le variabili di ambiente qua sotto per la basic auth

DHCORE_USER = "DHCORE_USER"
DHCORE_PASSWORD = "DHCORE_PASSWORD"